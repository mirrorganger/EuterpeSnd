name: Build and Test

on:
  push:
    branches: [ "feature/cmake_presets" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    # The CMake configure and build commands are platform agnostic and should work equally well on Windows or Mac.
    # You can convert this to a matrix build if you need cross-platform coverage.
    # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
    runs-on: ubuntu-24.04 
    strategy:
      matrix:
        compiler : [gcc, clang]
        configuraiton : [debug, release]

    steps:
    - name : install alsa 
      run  : sudo apt-get install -y libasound2-dev

    - name : install lcov
      run  : sudo apt-get install -y lcov

    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Configure CMake
      run: cmake --preset=${{matrix.compiler}}-${{matrix.configuration}}

    - name: Build
      run: cmake --build --preset=${{matrix.compiler}}-${{matrix.configuration}} 

    - name : Tests
      run : GTEST_COLOR=1 ctest --preset=${{matrix.compiler}}-${{matrix.configuration}} --verbose

    # - name : Create coverage report
    #   run : |
    #         lcov --directory ${{github.workspace}}/build/${{matrix.configuration}} --capture --output=${{github.workspace}}/build/${{matrix.configuration}}/lcov.info
    #         lcov --remove ${{github.workspace}}/build/lcov.info '*deps/*' '/usr/include/*' '*/*Test*' --output-file ${{github.workspace}}/build/lcov.info

    # - name: Upload coverage reports to Codecov
    #   uses: codecov/codecov-action@v4
    #   with:
    #     token: ${{secrets.CODECOV_TOKEN}}
    #     file:  ${{github.workspace}}/build/${{matrix.configuration}}/lcov.info
